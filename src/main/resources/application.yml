app:
  ldap:
    url: ${APP_LDAP_URL:ldap://localhost:389}
    base: dc=example,dc=org
    manager-dn: cn=admin,dc=example,dc=org
    manager-password: admin

logging:
  level:
    org.springframework.security: DEBUG