spring:
  application:
    name: cards
#  jpa:
#    generate-ddl: true
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  datasource:
    url: jdbc:postgresql://localhost:5432/payments
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 6
  mongodb:
    username: admin
    password: admin
    database: payments
    authentication-database: admin
    # host: localhost
    # port: 27017
  messages:
    basename: exceptions
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8100/realms/training
#------------------------------------------------------------
      client:
        registration:
          github:
            client-id: Ov23liR1a7hQXN59iCeF
            client-secret: ce09178fb052dd5d9e10bcd49a2d78c9363601aa
          keycloak:
            client-id: training-client
            scope:
              - openid
        provider:
          keycloak:
            issuer-uri: http://localhost:8100/realms/training
#  security:
#    user:
#      name: admin
#      password: admin
#      roles: ADMIN
logging:
  level:
    org.springframework: info
card-number-length: 16
secret: PeM7csCZ1546
api-keys: 00001,00002